# ═══════════════════════════════════════════════════════════════
# Nown - Environment Variables (Self-Hosted PostgreSQL)
# ═══════════════════════════════════════════════════════════════

# 🗄️  DATABASE (PostgreSQL)
DATABASE_URL="postgresql://nownuser:YOUR_DB_PASSWORD@localhost:5432/nown"
DIRECT_URL="postgresql://nownuser:YOUR_DB_PASSWORD@localhost:5432/nown"

# 🔐 JWT AUTHENTICATION
# Üretmek için: openssl rand -base64 64 | tr -d "\n"
JWT_SECRET="your-very-long-and-random-jwt-secret-key-here"

# ⚙️  NEXT.JS CONFIGURATION
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
NODE_ENV=development
PORT=3000

# 📝 NOT: Production'da deploy-standalone.sh bu değerleri otomatik oluşturur

